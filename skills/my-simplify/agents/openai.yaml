interface:
  display_name: "My Simplify"
  short_description: "Post-change cleanup on four angles, then apply the fixes"
  default_prompt: "Use $my-simplify to clean up the current diff for reuse, simplification, efficiency, and altitude, then apply the fixes."
