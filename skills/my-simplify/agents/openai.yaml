interface:
  display_name: "My Simplify"
  short_description: "Review recent code changes and apply focused simplifications"
  default_prompt: "Use $my-simplify to review the current diff for reuse, quality, and efficiency simplifications, then apply focused fixes."
