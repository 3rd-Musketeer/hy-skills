interface:
  display_name: "Closeup"
  short_description: "Close out a shipped session — teardown, restore, file, verify, report"
  default_prompt: "Use $closeup to close out this finished session: tear down scaffolding, restore borrowed environments, finalize status, file the decision, and hand off."
